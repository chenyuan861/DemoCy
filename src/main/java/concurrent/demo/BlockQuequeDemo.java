package concurrent.demo;

import java.util.HashMap;
import java.util.Map;
import java.util.concurrent.ArrayBlockingQueue;

/**
 * java.util.concurrent 包中有些参数
     * Blcokingqueque
     * concurrentHashMap，concurrentNagavitiverNamp
     * countDownLantch， CycliBarrier
     * Semaphore(acquire，relesase)
     * ExecutorService（线程池）
 * Atiomic
    * AtomicBoolean，AtomicInteger等
 * Locks
 *    锁机制
 */
public class BlockQuequeDemo {

    public static void main(String[] args) {
        ArrayBlockingQueue queue = new ArrayBlockingQueue(10);
        String s =null ;
        System.out.println(3&4);
        new HashMap<>();
        Map map = new HashMap<>();
       // queue.put(1);
    }



}
